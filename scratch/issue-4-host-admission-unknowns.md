# Issue #4 unknowns

| Unknown | Why it blocks planning | Type | Resolver | Disposition |
| --- | --- | --- | --- | --- |
| Which surface currently admits shape-valid sourced replacement without exact candidate binding? | Determines whether a prose-only change would satisfy the host boundary. | Existing behavior | Read-only capability inventory | **resolved** — native `planner` exposes every registry command and `NativePlannerEffectHost` calls `authorizePlannerOperations` with an empty authority; `replaceMealRecipeFromSource` is currently `ordinary`. `authorizeEmbeddedSourcedReplacements` has no runtime caller. Evidence: `lib/planner-tool-contract.ts`, `server/codex/planner-effect-host.ts`, `lib/household-command-contract.ts`, and `lib/sourced-recipe-contract.ts`. |
| Can the embedded host withhold this command without deleting direct informational replacement? | Determines whether the change can remain narrow and preserve the stated direct contract. | Existing behavior | Read-only capability inventory | **resolved** — HTTP/global/direct service routes validate the typed command independently, while `authorizePlannerOperations` is invoked only by the native embedded effect host. A distinct registry exposure can deny the embedded host and filter its model schema without changing the typed command/domain contract. |
| What proves checked source and installed candidate agree without household data or web content? | Determines RC/QA proof. | Existing proof framework | Read-only RC surface inventory | **resolved** — `npm run probe:release-lifecycle -- --public-command-rc` proves a clean committed candidate through disposable installed mounted QA; `npm run probe:codex-follow-up -- --no-auth --output …` proves installed skill/config/capability readback with a synthetic localhost provider and outbound rejection. |
